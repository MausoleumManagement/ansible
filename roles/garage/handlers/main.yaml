---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart garage
  ansible.builtin.systemd:
    name: "{{ garage_systemd_service }}"
    state: restarted
  become: true
